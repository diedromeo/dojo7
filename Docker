# ⚔️ Samurai CTF Dockerfile — Render-safe
FROM python:3.11-slim

# Set working directory
WORKDIR /app

# Copy requirements first (better caching)
COPY requirements.txt .

# Install Python deps (no cache, faster, safer)
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of your app
COPY . .

# Expose Render port (Render injects $PORT)
EXPOSE 5000

# Use gunicorn for production (Render auto-sets $PORT)
CMD gunicorn app:app --bind 0.0.0.0:${PORT:-5000} --timeout 120
